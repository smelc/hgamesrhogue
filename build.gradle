apply plugin: 'java'
apply plugin: "eclipse"

dependencies {
	    // Only for class 'Coord' which you can duplicate.
	    // Should not be considered a real dependency.
        compile project(":SquidLib")

        compile project(":com.hgames.lib")
}